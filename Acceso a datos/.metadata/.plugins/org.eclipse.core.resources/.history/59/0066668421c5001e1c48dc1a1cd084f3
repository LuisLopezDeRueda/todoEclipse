package OpenConection;

import java.util.ArrayList;

import org.bson.Document;

import com.mongodb.client.MongoCollection;
import com.mongodb.client.MongoDatabase;

import Modelo.Direccion;
import Modelo.Persona;

public class Test {
	public static void main(String[] args) {
		MongoDatabase db = MongoSession.getDataBase();
		MongoCollection<Document> c = db.getCollection("personas");
		Document document = new Document();
		document.append("nombre", "Luis");
		document.append("apellidos", "López de Rueda Laguna");
		document.append("dni", "2342423X");
		document.append("edad", 19);
		Persona perosna = new Persona();
		perosna.setApellidos("Lopez");
		perosna.setNombre("Luis");
		perosna.setDireccion(new ArrayList<Direccion>());
		perosna.getDireccion().add(new Direccion(1, "Avda Hola", 41930));
		c.insertOne(document);
	}
}
